
// /http://docs.mongodb.org/manual/reference/sql-comparison/

var SUPPORTED_CMDS = ["CREATE", "TABLE", "ALTER" ];

var LEVELONE_CMS   = ["CREATE", "ALTER", "SELECT", "INSERT" ];

function parseSQL(cmd, cb){
	
	
	
	
}
